import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.summertrainingmobile.app',
  appName: 'Summer Training Mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
